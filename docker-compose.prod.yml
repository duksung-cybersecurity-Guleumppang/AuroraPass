# AWS 운영 배포용 Docker Compose
# 사용법: 환경변수 설정 후 docker compose -f docker-compose.prod.yml up

services:
  backend:
    image: ${ECR_REGISTRY}/aurora-backend:${IMAGE_TAG}
    ports:
      - "${BACKEND_PORT:-${PORT:-8000}}:${BACKEND_PORT:-${PORT:-8000}}"
    environment:
      - PORT=${BACKEND_PORT:-${PORT:-8000}}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - AWS_DEFAULT_REGION=${AWS_REGION}
    read_only: true
    tmpfs:
      - /tmp:size=64m
    ulimits:
      nofile: 65536
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:${BACKEND_PORT:-${PORT:-8000}}/healthz || exit 1" ]
      interval: 10s
      timeout: 3s
      retries: 10
    restart: unless-stopped
    logging:
      driver: awslogs
      options:
        awslogs-group: /ecs/aurora-backend
        awslogs-region: ${AWS_REGION}
        awslogs-stream-prefix: ecs

  frontend:
    image: ${ECR_REGISTRY}/aurora-frontend:${IMAGE_TAG}
    ports:
      - "${FRONT_PORT:-${PORT:-3000}}:${FRONT_PORT:-${PORT:-3000}}"
    read_only: true
    restart: unless-stopped
    logging:
      driver: awslogs
      options:
        awslogs-group: /ecs/aurora-frontend
        awslogs-region: ${AWS_REGION}
        awslogs-stream-prefix: ecs
