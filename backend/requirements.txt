# Web Framework
fastapi
uvicorn[standard]
python-multipart
jinja2

# Validation & Environment
email-validator
python-dotenv

# Database & Cache
sqlalchemy
psycopg2-binary
redis

# Logging
structlog

# Audio Processing (for CAPTCHA synthesis)
librosa>=0.10.0
numpy>=1.21.0
soundfile>=0.12.0

# System dependencies (for librosa)
# Note: librosa requires system packages: libsndfile1, ffmpeg
# These should be installed in Dockerfile